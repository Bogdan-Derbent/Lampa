// Минимальный валидный плагин для Lampa
Lampa.Plugin.add('td_test_plugin', {
    // Эта функция запустится, когда плагин будет инициализирован
    init: function() {
        console.log('[TD Plugin]: Ура! Плагин загружен и инициализирован.');
        // Можно попробовать показать уведомление, чтобы убедиться визуально
        Lampa.Noty.show('Плагин TD работает!');
    }
});